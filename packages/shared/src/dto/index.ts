// DTO interfaces describing the wire contract. Populated per module; each
// backend DTO class declares `implements` against the interface here so that
// changing one without the other is a compile error.
export {};
